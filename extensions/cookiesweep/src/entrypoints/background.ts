import { defineBackground } from "wxt/utils/define-background";

export default defineBackground(() => {
  // Phase 5: tab-close / domain-leave triggers -> cleanup planner -> cookies.remove / browsingData.
});
