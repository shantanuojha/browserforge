{
  "name": "@browserforge/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared runtime helpers for BrowserForge extensions (storage, domain matching, logging, result types).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@webext-core/fake-browser": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
